import { CountyFilterPipe } from './county-filter.pipe';

describe('CountyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
